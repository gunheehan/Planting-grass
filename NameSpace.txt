using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 네임스페이스
// 클래스들을 묶어주는 개념.

// 유니티에서는 만들어진 클래스들을 네임스페이스로 묶어 공유하고,
// 적용한 프로젝트의 클래스  파일들과 구분할 수 있게 됨.

// 외부에서 클래스에 접근하려면 네임스페이스를 추가해야 가능.

namespace NamespaceTest
{
    class 오로나민C
    {
        public int a;
    }
    public class HelloClass09Namespace : MonoBehaviour
    {

        // Start is called before the first frame update
        void Start()
        {

        }
    }

}
